@AbapCatalog.viewEnhancementCategory: [#NONE]
@AccessControl.authorizationCheck: #NOT_REQUIRED
@EndUserText.label: 'Figure Development'
@Metadata.ignorePropagatedAnnotations: true
@ObjectModel.usageType:{
    serviceQuality: #X,
    sizeCategory: #S,
    dataClass: #MIXED
}
define view entity ZRAPH_##_I_OVPFigureDevelopmnt
  as select from /DMO/I_Travel_M as travel
{
  key travel.agency_id as AgencyID,
      travel.currency_code as CurrencyCode,
      @Semantics.amount.currencyCode: 'CurrencyCode'
      sum(travel.total_price) as Price,
  
      _Agency.Name as AgencyName
}
where travel._Agency.CountryCode = 'US'
group by travel.agency_id, travel.currency_code, _Agency.Name
